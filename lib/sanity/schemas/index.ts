// Sanity Schema Index

import playlist from './playlist';
import collection from './collection';
import siteConfig from './siteConfig';

export const schemaTypes = [playlist, collection, siteConfig];

export default schemaTypes;
